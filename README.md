# AI Stack CPU Only – Stack Generator

This repository contains the `setup_stack.py` generator for creating a complete
CPU-only AI stack that includes:

- Ollama (LLM inference)
- Open-WebUI (frontend)
- ComfyUI (image generation)
- Wyoming-Piper (TTS engine)
- Optional TTS HTTP Adapter
- Modular installer shell scripts
- Docker-Compose orchestration

The generator script builds a full versioned stack directory such as:

So far this only creates the continers, much config work still to be done.

